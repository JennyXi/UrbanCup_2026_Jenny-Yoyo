# Optional dependencies for rebuilding data_collection/ outputs.
# The core simulation, unit tests, and competition report use only the standard library.
pandas>=2.2
pyarrow>=15
requests>=2.31
beautifulsoup4>=4.12
pytest>=8
